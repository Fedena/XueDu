<div id="content-header">
  <img src="/images/HR/show_hr.png" alt="" />
  <h1><%= t 'Payroll' %></h1>
  <h3><%= t 'Add_category' %></h3>
  <div id="app-back-button">
    <%= link_to_function image_tag("/images/buttons/back.png",:border => 0), "history.back()" %>
  </div>
</div>

   <div id="page-yield">

  <% unless flash[:notice].nil? %>
    <p class="flash-msg"> <%= flash[:notice] %> </p>
  <% end %>

  <% form_for :category do |c| %>
      <%= error_messages_for 'category' %>

    <div id="form-content">
      <div class="label-field-pair">
        <label for="category_name">类别名称</label>
        <div class="text-input-bg"><%= c.text_field :name %></div>
      </div>

      <div class="label-field-pair">
        <label for="percentage">百分数</label>
        <div class="text-input-bg"><%= c.text_field :percentage %></div>
      </div>

      <div class="label-field-pair">
        <label for="percentage_of">百分比</label>
        <div class="text-input-bg">
          <%= select :category, :payroll_category_id, @categories.map {|cat| [cat.name, cat.id]}, :prompt =>"选择" %>
        </div>
      </div>
 <div class="label-radio-pair">
      <label class="status" for="status">状态</label>
<div id="radio-buttons">
      <%= c.radio_button  "status", "true", :checked => "checked" %>
      <label class="label_for_status" for="add_grade_status_true">激活</label>

      <%= c.radio_button  "status", "false" %>
      <label class="label_for_status" for="add_grade_status_false">禁用</label>
    </div>
    </div>
      <div class="label-checkbox">
        <%= c.check_box :is_deduction %> 是否扣除 
      </div>
<div class="extender"></div>
   
    <div id="submit-button">
      <%=submit_tag("", :value => "► 保存") %>
    </div>


      </div>
  <% end %>

  <div id="category-list">
    <%= render :partial=>"category" %>
  </div>

   </div>
